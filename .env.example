# UpayaNode – copy to .env and set values (do not commit .env)
# Used by docker-compose and optionally by local run.

# Required in production
SECRET_KEY=your-secret-key-min-50-chars
DEBUG=0
ALLOWED_HOSTS=localhost,127.0.0.1,.yourdomain.com

# Redis (default works with docker-compose redis service)
REDIS_URL=redis://redis:6379/1

# Optional: database (default is SQLite in container)
# DATABASE_URL=postgres://user:pass@db:5432/upayanode
